{
    ## 作为连接rabbitMQ Server的客户端的配置
    "master": {
        ## rabbitMQ的地址
        "server": "rabbitmq.debade",
        "port": 5672,
        "user": "guest",
        "password": "guest"
    },
    ## 依赖redis
    "redis": {
        "host": "redis.debade",
        "port": 80,
        "channel": "debade"
    }
}
